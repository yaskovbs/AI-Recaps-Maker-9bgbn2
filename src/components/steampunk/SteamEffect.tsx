import React from 'react';

// Legacy shim — SteamEffect not needed in new design
export default function SteamEffect({ count = 5, className = '' }: { count?: number; className?: string }) {
  return null;
}
